#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    int y;
    scanf("%d",&y);
    for(int i=0;i<n;i++){
      if(y==a[i]){
         printf("the target value %d at position%d",y,i+1);
         found =1;
         break;
      }  
    }
    if(found==0){
        printf("target value not found");
    }
    
}
